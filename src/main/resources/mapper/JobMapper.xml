<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tsoft.sched.quartz.mapper.JobMapper">

    <!-- Define your SQL queries here -->

    <insert id="insertJobManage" parameterType="com.tsoft.sched.quartz.dto.JobManage">
        INSERT INTO TB_JOB_MANAGE (JOB_TITLE, JOB_BODY, JOB_START_DATE, JOB_STATUS, JOB_REG_DATE)
        VALUES (#{jobTitle},#{jobBody},#{jobStartDate}, #{jobStatus}, now())
    </insert>
</mapper>
